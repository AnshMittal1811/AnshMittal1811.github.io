# AnshMittal1811.github.io

Static GitHub Pages media archive for Ansh Mittal.

This deployment keeps the personal-website project videos in-repo under `assets/videos/`
and ports selected image/media assets under `assets/images/`. The large top-panel astronomy
MP4 from the newer personal website is intentionally excluded so the homepage remains light.

The site is plain static HTML/CSS/JS and deploys directly from the `main` branch.
